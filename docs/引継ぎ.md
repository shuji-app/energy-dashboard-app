# エネルギー管理ダッシュボード — 引継ぎ.md

このドキュメントは、Claude Codeとの対話で `energy-dashboard-app` を一から作った際の
実装履歴・設計判断・データ品質問題の調査経緯をまとめたものです。今後このアプリに手を入れる際は
（Claude Codeによる作業を含め）必ず目を通してください。「なぜこうなっているか」が分かるように
書いています。

---

## 1. このアプリは何か

設備（吸収式冷凍機＋ボイラー）の日次エネルギーデータをもとにした、単一ページのエネルギー管理
ダッシュボードです。元データは `エネルギー.xlsx`（日付ごとの外気温・湿度、冷水/温水熱負荷、
購入電力量、重油使用量の記録）。以下の3モードを持ちます。

- **ダッシュボード**：日/月/年の期間切替、複数グラフ（項目・グラフ種別を自由に設定可能）、
  前期比較、予測（月内の残り日数・年度残り月数を推定）、KPIカード（電力量・重油・原油換算・
  デマンド・概算コストなど）。
- **データ入力**：カレンダーからの日次入力フォーム（前日/1週間前の参考値表示、バリデーション、
  Enterキーで保存→翌日へ移動、前日値コピー、メモ入力）、入力済み一覧のCSV的テーブル表示、
  メモ一覧、未入力日一覧、Excel再取込（データ更新）。
- **分析**：原単位トレンド、異常値検知（メモへの記録可）、号機別稼働バランス、目標比較、
  カレンダーヒートマップ、CO2トレンド、デマンド分析（ヒストグラム）、複数年度重ね表示。
  各グラフにPNG画像保存ボタンあり。

その他：設定（換算係数・単価・しきい値など全パラメータ変更可）、バックアップ/リストア
（JSON書き出し/読み込み）、ダークモード、印刷用レポート（`window.print()` + 印刷専用CSS）、
PWA対応（ホーム画面インストール・オフライン起動）。

## 2. アーキテクチャの前提（重要）

- **ビルドツールなし。** `app.jsx`は素のReact（グローバルの`React`/`ReactDOM`を直接使用、
  `const { useState, useEffect, useMemo, useRef, useCallback } = React;`）で書かれており、
  ES moduleのimport/exportは一切使いません。
- React 18.3.1 / ReactDOM 18.3.1 / Chart.js 4.4.4 / xlsx(SheetJS) 0.18.5 は全て
  `index.html`内でバージョン固定のCDN URLから読み込みます。
  - Chart.jsだけは cdnjs では404になるため **jsdelivr** (`cdn.jsdelivr.net/npm/chart.js@4.4.4/...`)
    を使っています。他のライブラリをcdnjsからjsdelivrに変える必要はありません。
- `build.js`は`app.jsx`をBabel（`@babel/preset-react`、classic runtime）でJSX→プレーンJSに
  変換して`app.compiled.js`に書き出すだけの薄いスクリプトです。
- データは初回`data/energy-data.js`（`window.ENERGY_DATA=[...]`）から読み込み、以後は
  `localStorage`に保存されます。バックエンドはありません。
- `ChartCanvas`という共通コンポーネントが全グラフの描画を担い、`useEffect`（依存配列は空）で
  Chart.jsインスタンスをマウントします。設定オブジェクトには関数（tooltipコールバック等）が
  含まれ単純比較できないため、diffの代わりに文字列化した`key`をpropとして渡し、内容が変わったら
  Reactに強制的にアンマウント→再マウントさせる方式を採っています（ダークモード切替時に
  `Chart.defaults`のグローバル設定を反映させるためにも、`darkMode`を必ず`key`に含めています）。

## 3. データについて

### 3.1 スキーマ
1日1レコード：日付、外気温、湿度、冷水熱負荷(GJ)、温水熱負荷(GJ)、購入電力量(kWh)、
重油使用量(L、冷凍機用/ボイラー用など号機別)。ここから`withDerived()`で
デマンド(kW)・合計熱負荷(GJ)等を導出します。

### 3.2 データ破損の調査と対応（重要・恒久対応済み）
元の`エネルギー.xlsx`のSheet1で、Excel行247〜488付近に**日付が非連続（後退・重複）で
ハードコードされた**（数式ではなく固定値として入力された、`.Formula == .Value2`で確認）
242行分のセルが見つかりました。

- 調査手順：全行の日付を`toDays()`的なヘルパーで差分チェックし、後退ジャンプが4箇所あることを
  特定 → 該当する242行の連続区間を特定。
- 当初はユーザーの判断で「ひとまず除外して進める」ことにしましたが、その後さらに精査した結果、
  242行のうち**91行（2024-12-01〜2025-03-01）は数値も妥当でかつ内部的に日付が連続していた**
  ため、この91行だけをデータセットに復元しました。残り151行は本当に破損しており、恒久的に
  除外しています（`data/excluded-rows.json`に理由付きで保管、アプリ本体は読み込まない）。
- 最終的な正データ：`data/energy-data.json`、721件、2024-04-01〜2026-03-31、この範囲に
  日付の欠落なし（ただし2025-10-01〜03、2025-12-01〜04、2026-01-01〜02など、合計9日分の
  小さな「本当に未入力」の欠落は残っています＝これはデータ破損ではなく単純未記入）。
- **再発防止策**：「データ更新」（Excel再取込）機能内に、取込中の行の日付が
  それまでに処理済みの最大日付より後退していたらその行をスキップするガード
  （`DataUpdateModal.handleFile`内）を追加済み。同じ破損が将来の再取込で紛れ込むのを防ぎます。

このデータの経緯があるため、**数値がおかしく見えても勝手に補正しないこと**。まず元データ
(`エネルギー.xlsx`本体、または`data/energy-data.json`)を確認し、必要ならユーザーに確認する
運用にしています（下記「6. 未解決の問題」も参照）。

### 3.3 localStorageキー一覧
| キー | 内容 |
|---|---|
| `LS_DATA_KEY` | 日次データ本体（配列。Excel再取込でマージ/upsert-by-date） |
| `LS_SETTINGS_KEY` | 設定（換算係数・単価・しきい値など） |
| `LS_VIEWSTATE_KEY` | 表示状態（mode/charts構成/選択中の期間など） |
| `LS_NOTES_KEY` | メモ（日付→テキストのマップ） |
| `LS_DARKMODE_KEY` | ダークモードON/OFF |

実際のキー文字列名は`app.jsx`冒頭の定数定義を参照してください。

### 3.4 設定項目（`DEFAULT_SETTINGS`）
契約電力(kW)、重油発熱量(MJ/L)、電力→MJ換算係数、電力CO2排出係数、重油CO2排出係数、
目標電力量(kWh)、目標重油使用量(L)、原油換算係数(GJ/kL)、異常値検知のσしきい値、
デマンド警報しきい値(%)、電力単価(円/kWh)、重油単価(円/L)。すべて設定画面から変更可能。

## 4. 実装の経緯（機能追加の流れ）

会話の中で以下の順で機能を積み上げました（詳細な各回のユーザー指示は省略し、実装された
機能のまとまりを記載）。

1. 元データ分析 → `energy-dashboard-app-prompt.md`（設計プロンプト）作成 → アプリ一式を実装。
2. データ入力に前日値の参考表示を追加。
3. ダッシュボードのグラフ・項目選択UIをレイアウト調整（グラフ左・項目選択右、2グラフ常設、
   項目未選択時は空白、複数項目選択時は同一グラフに重ね描画）。
4. グラフごとの設定（グラフ種別・項目）をモーダル化、平日/休日の色分け対応、前期比較を
   グラフヘッダーに追加、グラフヘッダーに外気温・湿度を追加。
5. コメント/項目増加時のレイアウト可変対応。データ更新（Excel再取込）の反映不具合を修正。
6. 分析機能の提案 → 承認を得て一括実装（原単位トレンド、異常値検知、号機別バランス、目標比較、
   ヒートマップ、CO2トレンド、デマンド分析、複数年度重ね表示など）。
7. ダッシュボードに原油換算値・2行ヘッダー（1行目タイトル、2行目各項目、設定ボタン右端）を追加。
8. データ入力画面に入力済み一覧（CSV形式テーブル、計算済み項目も含む）を追加。
9. UX改善の提案 → 承認を得て一括実装（入力バリデーション、Enterキーで次の日へ、メモ一覧、
   しきい値の設定可能化、異常値のメモ記録、複数年度重ね表示、PWA対応）。
10. さらなる改善提案 → 一部（概算コスト表示、PNGエクスポート、アラート拡張、未入力日一覧、
    印刷用レポート）を実装。**「特定曜日への一括入力」は明示的にユーザーが除外を指示したため
    未実装**（今後実装する場合は改めて要望を確認すること）。
11. Service Workerのキャッシュ不具合を発見・修正（詳細は次節）。
12. ダッシュボードの期間切替UIを1つの枠に統合（日/月/年タブ＋期間ピッカーを同一フレーム化）、
    「実績」ラベルを大型化（クリック誘導文言は削除）、グラフの拡大表示モーダル内でも
    グラフ設定（グラフ名・グラフ種別・表示項目）を直接変更できるように変更
    （`CHART_TYPE_OPTIONS`定数を新設し`ChartSettingsModal`と拡大モーダルで共有）。
13. 実績ラベル行に期間送り矢印（◁▷）を追加（日表示は月単位、月表示は年度単位で移動。
    `stepPeriod()`関数を新設）。カッコ内の入力済み日数表記は13pxに戻し、他はそのまま。
14. **タブの再編成**：3タブ（日/月/年）を2タブに変更し、表記を「日→月」「月→年」に変更。
    元の「年」タブ（最新年度固定・複数年比較用のview="year"）はUIから削除（内部の
    `view==="year"`分岐やbuildYearViewBuckets自体は未使用コードとしてapp.jsxに残っている）。
    保存済みviewStateのviewが"year"だった場合は"day"にフォールバックするガードを追加。
    KPIカードの「前期比」表示を、% と (前期 実数値) の2行表示に変更（`.kpi-prev`をblock化）。
15. 「データ最終日: ...」表示をヘッダーのタイトル行（`app-title-row`）に移動し、タイトルの
    右横に並べるよう変更（元は2行目のモード切替ボタン列と同居していた）。
16. 拡大表示モーダルの改修：外気温度/湿度/予測を表示/平日休日で色分け/前期比較のトグル行を
    グラフ本体と同じ内容で追加し、最終的にインラインのグラフ種別・表示項目パネル
    （`fullscreen-modal-settings`）は削除して、「⚙ グラフ設定」ボタン経由で
    `ChartSettingsModal`をモーダル内モーダルとして開く形に統一（グラフが大きく見えるように）。
17. 分析ビューに3パネルを追加（ユーザーへの提案リストから1・4・8番を選定して実装）：
    - **9. 気温感応度分析**：年度ごとに`buildRegression(rows, metric)`で気温回帰の傾きを算出し、
      年度別の棒グラフで表示。傾きが年々増えていれば温度依存の効率低下のサイン。
    - **10. 曜日別パターン**：選択年度の対象指標を曜日ごと（月〜日）に平均し棒グラフ表示。
      土日はDAY_TYPE_COLORの色を流用して視覚的に区別。
    - **11. 年間目標への累積進捗**：既存の「4. 目標比較」（単一スナップショットのバー）とは別に、
      月次累積実績と、日数按分した目標ペース（直線）を重ねた折れ線グラフを追加し、
      年度途中でも目標に対して進んでいる/遅れているかを視覚的に把握できるようにした
      （`targets`配列に`key`フィールドを追加して月次sumMetricで再利用）。
18. **KPIカード表示項目（総合熱源効率・CO2排出量・原油換算値・概算コスト）をグラフ化可能に**。
    これらは元々`efficiency` useMemo内で期間合計から直接計算するだけの表示専用値だったが、
    設定（単価・CO2係数・原油換算係数等）に依存する値を日次で再計算する`withComputed(row, settings)`
    を新設し、`METRICS`に`efficiencyPct`/`co2T`/`crudeOilKL`/`costYen`の4項目を追加（新グループ
    「コスト・環境」）。App内で`dataDisplay = useMemo(() => data.map(r => withComputed(r, settings)), [data, settings])`
    を作り、ダッシュボードのbuckets/computeForecastMap/buildMainChartConfigのallRows、
    `DataEntryView`・`AnalysisView`への`data`propは全てこの`dataDisplay`に差し替え（生データ
    `data`自体は保存・インポート処理でのみ使用し続けるため、設定変更時も常に最新の換算結果が
    グラフ・データ入力の一覧・分析の各ドロップダウンに反映される）。KPIカードの当該4項目にも
    `active`/`onClick`を付与し、他の項目と同様にクリックでグラフ1にトグルできるようにした。
    `co2T`/`crudeOilKL`/`costYen`はsum系のため`FORECASTABLE`にも追加（`efficiencyPct`は比率の
    ためdemandKWと同様forecastableから除外）。`ENTRY_LIST_COLUMNS`と`ANALYSIS_METRIC_CHOICES`
    にも4項目を追加済み。
19. **「レポート作成(PDF)」を画面内プレビュー方式に変更**：従来はボタン押下で即`window.print()`
    していたが、いきなり印刷ダイアログが開くのは確認が取れず不便なため、まず`showReportPreview`
    状態をONにして`.app-shell`に`report-preview`クラスを付与 → 画面上で実際の印刷レイアウト
    （`@media print`と同じ内容のタブ/設定ボタン等を隠し、`.print-only-header`を表示）に
    ライブ変換して見せるようにした。印刷用CSSルールを`.app-shell.report-preview ...`という
    非@media版として複製（`.print-report-title`/`.print-report-meta`の見た目は元々@media print
    内にしかなかったため、常時適用のルールへ引き上げて共通化）。画面上部に固定の
    `.report-preview-bar`（`no-print`付き＝実際の印刷では映らない）を表示し、
    「🖨 印刷する」（`window.print()`実行）と「✕ プレビューを閉じる」を配置。
20. **ローカルファイルへの自動保存機能を追加**（File System Access API、設定画面の新セクション）。
    localStorageはブラウザのキャッシュ削除等で消えるリスクがある、という話の流れでユーザーが
    選んだ方式（他の選択肢：Firebase等クラウドDB連携、手動バックアップの定期運用）。
    - `window.showSaveFilePicker()`でユーザーがPC上のJSONファイルを1つ選択/新規作成すると、
      以後`data`/`settings`/`notes`のいずれかが変わるたびに、既存の「バックアップ」機能と
      同じペイロード形式（`buildBackupPayload()`に共通化）でそのファイルへ自動上書き保存する
      （`useEffect`の依存配列に`fileHandle`を含めることで、連携直後の初回保存も兼ねる）。
    - `FileSystemFileHandle`は`localStorage`に保存できない（構造化複製はできるがJSON化できない）
      ため、IndexedDB（`idbOpen`/`idbSaveHandle`/`idbLoadHandle`/`idbClearHandle`、DB名
      `energyAppFS`）に保管し、次回起動時も同じファイルへ書き込み続けられるようにした。
    - 再起動時は`handle.queryPermission()`で権限を確認し、`granted`ならそのまま自動保存を再開。
      それ以外（`prompt`）の場合はブラウザの制約上ユーザー操作なしに`requestPermission()`を
      呼べないため、「再連携する」ボタンを表示してユーザーのクリックをトリガーに許可を取り直す。
    - Chromium系ブラウザのみ対応（`"showSaveFilePicker" in window`で機能検出、非対応ブラウザには
      その旨を表示するのみで他の設定項目には影響しない）。
21. **Firebase Realtime Databaseによる複数端末（iPad＋PC等）リアルタイム同期を追加**。
    「iPadとデスクトップで同期を取りながら使いたい」という要望に対し、`ac-check-app`が既に
    同じ用途（iPad+PCのリアルタイム共有）でFirebase Realtime Databaseを使っており実績があった
    ため、同じ設計パターンを移植した（`ac-check-app`の`app.jsx`を事前に調査し、実装を忠実に
    踏襲）。ただし書き込み方式は本アプリ向けに整理し直している（詳細は下記）。
    - **SDK**：`firebase-app-compat.js` + `firebase-database-compat.js`（v10.14.1、CDN、
      `index.html`で`app.compiled.js`より前に読み込み）。modular SDKではなくcompat SDK
      （`window.firebase.initializeApp(...)`スタイル）を使用（ac-check-appと同じ）。
    - **`firebaseConfig`**：`app.jsx`冒頭（`DEFAULT_CHARTS`の直後）にプレースホルダー値
      （`apiKey: "YOUR_API_KEY"`等）で定義。実際の値は、ユーザー自身がFirebaseコンソールで
      新規プロジェクトを作成して取得する必要があるため（Googleアカウントでのログイン・
      プロジェクト作成はエージェントが代行できない操作）、README.mdに手順を追記し、
      ユーザー側で`firebaseConfig`を書き換えて`npm run build`してもらう運用とした。
      `apiKey !== "YOUR_API_KEY"`かつ`window.firebase`が存在する場合のみ`db`が有効になり、
      それ以外（未設定・CDN読み込み失敗等）は`db = null`となって同期処理は全てスキップされ、
      アプリは今まで通りlocalStorageのみで動作する（ac-check-appと同一のガード条件）。
    - **Realtime DBのパス構成**：`energyData/{date}`（1日1レコード）、`settings`（設定オブジェクト
      まるごと）、`notes/{date}`（メモ）の3ノード。ac-check-appの「点検回」のような入れ子構造は
      不要なため、よりシンプルなフラット構成にした。
    - **読み取り（他端末の変更を受信）**：`data`/`settings`/`notes`それぞれに対して、マウント時
      （空の依存配列）に`ref.on("value", handler)`を張る3本の`useEffect`。スナップショットが
      存在すれば`setData`/`setSettings`/`setNotes`とlocalStorageキャッシュを更新、存在しなければ
      （＝新規Firebaseプロジェクトでまだ何も無い場合）その時点のローカルデータを初期シードとして
      アップロードする（`fbBulkSaveData(data)`等）。
    - **書き込み（自分の変更を送信）**：ac-check-appの設定まわりは「stateの変化を監視する
      useEffectで書き込む」方式だったが、これは「リモートから受信→ローカルstate更新→
      それを監視するeffectが再度リモートへ書き込む」という不要な往復（エコー書き込み）が
      発生し得る設計だったため、本アプリでは踏襲しなかった。代わりに、実際の保存操作を行う
      各ハンドラ（`handleSaveRecord`/`handleDeleteRecord`/`handleSaveNote`/`handleSaveSettings`/
      `handleImportData`/`handleImportBackup`）の中から直接
      `fbSaveRecord`/`fbDeleteRecord`/`fbSaveNote`/`fbSaveSettings`/`fbBulkSaveData`/`fbReplaceAll`
      を呼ぶ構成にし、"読み取り用effect"と"書き込み"を完全に分離した（読み取り側のsetStateが
      書き込みを誘発することは構造的に起こらない）。
      - `fbSaveRecord`/`fbDeleteRecord`/`fbSaveNote`：1日単位の`set()`/`remove()`（通常の保存・削除操作）。
      - `fbBulkSaveData`：Excel再取込（`handleImportData`）で追加/更新された行だけを
        `db.ref().update({...})`でまとめて送る（変更の無い行は送らない）。
      - `fbReplaceAll`：「バックアップから復元」時専用。energyData/settings/notesの3ノードを
        丸ごと置き換える（復元＝「これが正」の全置換という意味合いのため、増分updateではなく
        `set`相当の完全上書きにしている）。
    - ヘッダーの「データ最終日」の右に、`db`が有効なときだけ「🔥 同期中」バッジを表示
      （`db`はモジュールスコープの定数のため、追加のstateなしで判定可能）。
    - デバウンス処理は入れていない（ac-check-appと同じ判断：レコード保存はボタン押下等の
      明示的操作の時だけ発生し、キー入力のたびに書き込まれるものではないため）。
    - エラーハンドリングは`db.ref(...).set()`等をfire-and-forgetにしている点もac-check-appを踏襲
      （書き込み失敗時のUI通知は無し。将来的に必要になれば追加を検討）。
22. **GitHub Pagesでの公開＋Firebase Authenticationログインゲートを実装 → その後、認証は
    一時的に無効化（現状はお試し運用のため）**。
    - リポジトリを公開（`https://github.com/shuji-app/energy-dashboard-app`）してGitHub Pages
      （`https://shuji-app.github.io/energy-dashboard-app/`）で公開する運びになったため、
      「ソースが公開される＝`firebaseConfig`も公開される」という前提で、いったんは
      `AuthGate`コンポーネント（Firebase Authのメール/パスワードでサインインするまで
      `<App/>`をレンダリングしないゲート）を実装し、ルート描画を`<AuthGate/>`にしていた。
    - しかしユーザーから「今はお試しなので、ユーザー制限は設けない」との指示があったため、
      ルート描画を`<App/>`に戻し（`ReactDOM.render/createRoot`の引数を`<AuthGate/>`→`<App/>`に
      変更）、ログイン必須の状態を解除した。**`AuthGate`コンポーネント自体・`auth`初期化・
      `firebase-auth-compat.js`の読み込みはコードに残したまま**にしてある
      （実装済みで動作確認も取れているため、将来「本番運用に切り替えたい」となった際は
      `<App/>`を`<AuthGate/>`に戻すだけで再度有効化できる）。
    - Firebase Realtime DatabaseのルールもREADME記載の`auth != null`版には変更しておらず、
      引き続き`{".read": true, ".write": true}`の全公開ルールのまま運用している
      （＝現状は「ソースコードもDBも、URLとAPIキーを知っていれば誰でも読み書きできる」状態。
      お試し運用の間だけの想定で、本運用に進める際はREADMEの「Firebaseの初回セットアップ」
      手順4に従って認証必須のルールに変更し、`<AuthGate/>`を再度有効化することを強く推奨する）。
    - GitHubへのアップロードは、この環境からの`git push`が対話的認証（ブラウザでのOAuth確認）を
      必要とし自動化できなかったため、`git archive HEAD`で.gitignore適用済みのファイル一式を
      別フォルダに展開し、ユーザーがGitHubの「Upload files」機能でドラッグ＆ドロップする方式で
      行った（`git push`によるアップロードではない。以後コードを更新した場合も、同様に
      `git archive`で展開したフォルダの中身を再アップロードしてもらう必要がある）。
23. **iPadでの利用に向けたレスポンシブ対応の不具合修正**。実際にブラウザの表示幅をiPad相当
    （768〜1180px、縦横両方）に変えて検証したところ、データ入力画面でのみ横スクロールが
    発生する不具合を発見・修正した。
    - 原因：`DayEntryForm`の`.entry-form-header-row`/`.entry-form-row`が
      `grid-template-columns:1fr 90px 90px 156px`という固定幅（先週同曜日/前日実績/本日入力の
      3列で合計336px+gap）を常に確保していたため、`.entry-form-panel`が狭くなる幅
      （iPad縦向きで`.entry-layout`がflex-wrapして1カラムになった直後あたり）でラベル列が
      潰れきれず、ページ全体がわずかに（4〜20px程度）横に伸びてしまっていた。
    - 気づいた経緯：`.entry-list-scroll`（項目一覧のCSVテーブル、`overflow-x:auto`で意図的に
      横スクロールする作り）を疑って最初は誤検知したが、`getBoundingClientRect()`で実際の
      描画位置を確認したところ、これは`overflow-x:auto`により正しくコンテナ内に収まっており、
      本当の原因は`.entry-form-row`だと判明した（`scrollWidth`だけで overflow を判定すると、
      意図的な横スクロールコンテナまで誤検知するので注意——今後同様の調査をする際は
      `getBoundingClientRect().right > window.innerWidth`で見た方が確実）。
    - 対応：`@media (max-width: 900px)`で該当の固定列幅を縮小（`156px`等→`112px`等）、
      入力欄の幅も`110px`→`78px`に縮小。また保険として`index.html`側の`html, body`に
      `overflow-x:hidden`を追加（今後同種の微小オーバーフローがあってもページ全体の
      横スクロールを防ぐ）。
    - あわせて`index.html`にiOS向けのホーム画面追加用metaタグ
      （`apple-mobile-web-app-capable`、`apple-mobile-web-app-status-bar-style`、
      `apple-touch-icon`）を追加し、iPad SafariでAdd to Home Screenした際にアドレスバー無しの
      アプリ風表示になるようにした。
24. **中央監視装置の熱源画面（PDF/JPEG/PNG）の取り込み・プレビュー機能を追加**。
    - ヘッダーに「🖥 中央監視画面取り込み」ボタンを追加（`データ更新`ボタンの隣）。押すと
      `MonitorImportModal`が開き、対象日とファイル（PDF/JPEG/PNG、`MONITOR_FILE_TYPES`で
      MIME種別チェック）を指定して保存する。
    - **保存先はIndexedDB（この端末のみ、Firebaseには同期しない）**。既存の自動保存機能
      （File System Access API）と同じ`energyAppFS`データベース内に、新しいオブジェクトストア
      `monitorFiles`（keyPath: `date`）を追加する形で実装（`IDB_VERSION`を1→2に上げ、
      `onupgradeneeded`で両方のストアを条件付きで作成するようにした。既存ユーザーが
      バージョン2で開いた際、`handles`ストアは維持されたまま`monitorFiles`だけが追加される）。
      値は`{date, fileName, fileType, blob: File, uploadedAt}`で、Blob/File はIndexedDBに
      そのまま格納できる（base64変換等は不要）。
    - **プレビューは「データ入力」画面のカレンダーから**：ファイルがある日には🖥アイコンを
      日付セルの左上に表示（メモの📝アイコンは右上、重ならないよう配置を分けた）。🖥アイコンを
      クリックすると`MonitorPreviewModal`が開き、画像なら`<img>`、PDFなら`<iframe>`で表示する
      （アイコンのクリックは`stopPropagation()`で、セル本体のクリック＝日付選択とは独立させている）。
      あわせて`entry-cal-summary-actions`（未入力日一覧・📝メモ一覧の並び）に
      「🖥 中央監視画面一覧」ボタンも追加し、`MonitorFilesListModal`で全件を日付順に一覧表示・
      プレビュー・削除できるようにした。
    - 一覧のロード時はBlob本体を含めず`{date, fileName, fileType, uploadedAt}`のみ扱う
      ことで、ファイル数が増えてもメモリを圧迫しないようにしている（実体（Blob/Storageの
      ダウンロードURL）はプレビュー時に個別取得する）。
25. **中央監視画面を他端末とも共有できるようにし、アップロードをドラッグ＆ドロップ対応に変更**
    （前項の続き。同じ会話の中でユーザーから「他端末と同期を取りたい」「ドラッグ可能にして
    ほしい」との追加要望があり対応）。
    - **ドラッグ＆ドロップ対応**：`MonitorImportModal`に`.monitor-dropzone`（点線ボーダーの
      クリック可能な領域）を追加し、`onDragOver`/`onDragLeave`/`onDrop`を実装。
      `onDragOver`で`e.preventDefault()`を呼ばないとブラウザ既定の「ファイルを開く」動作に
      なってしまう点に注意。ドロップされたファイルもクリックで選択したファイルも同じ
      `acceptFile(file)`関数でMIMEタイプ検証（`MONITOR_FILE_TYPES`）とstate更新を行う、
      という単一の入力経路に統一した。
    - `App`直下に`monitorFiles`（一覧）stateを持ち、`DataEntryView`にはpropとして渡すだけの
      構造にした（旧`monitorRefreshTick`方式は廃止）。同期ありの場合は`App`が
      `on("value")`でリアルタイム購読、同期なし（IndexedDBのみ）の場合は`monitorTick`
      カウンタで手動リロード（ヘッダーからの保存・削除のたびにインクリメント）。

    **他端末同期の保存先について、実装を1往復やり直した経緯**（重要・今後の参考に）：
    1. 最初はFirebase Storageに画像本体を保存し、Realtime Databaseにはメタ情報
       （`monitorFilesMeta/{date}`）だけを置く一般的な構成で実装した。
    2. ところが実機確認で、**Firebase Storageを有効化（コンソールで「始める」）していない状態で
       `put()`/`getDownloadURL()`を呼ぶと、エラーにならず無期限にハングする**ことが判明
       （約8秒待っても応答無し）。放置するとアップロードモーダルが「保存中…」のまま
       永遠に固まってしまう。
    3. さらにユーザーがFirebaseコンソールでStorageを有効化しようとしたところ、
       **「Storageを使用するには、プロジェクトの料金プランをアップグレードしてください」**
       と表示された。これはGoogleの2024年10月の方針変更により、**新規Firebaseプロジェクトの
       Firebase Storageは無料プラン（Spark）では使えなくなり、有料のBlazeプラン（従量課金・
       クレジットカード登録必須）へのアップグレードが必須になった**ため。カード登録は
       ユーザー自身の判断が必要な操作（エージェントが代行できない）であり、選択肢として
       ユーザーに確認したところ、**「Realtime Database（無料）にbase64で保存」を選択**。
    4. そのため**Firebase Storage関連のコードは全て削除し、Realtime Database
       （既に無料で使えている）に画像・PDFをbase64文字列として保存する方式に作り直した**：
       - `index.html`から`firebase-storage-compat.js`の読み込みを削除。
       - `storage`変数・`window.firebase.storage()`初期化を削除。
       - `monitorSyncEnabled = !!db`（Realtime Databaseだけで判定、Storageは無関係に）。
       - `monitorSaveFile`：`FileReader.readAsDataURL()`でファイルをdata URL文字列に変換し、
         `monitorFileBlobs/{date}`にその文字列を`set()`。あわせて`monitorFilesMeta/{date}`に
         `{fileName, fileType, uploadedAt}`を`set()`（一覧表示用の軽量なメタ情報と、
         実体のdata URLを別ノードに分離。一覧の`on("value")`購読は`monitorFilesMeta`だけを
         見るので、ファイル本体を毎回全端末にダウンロードさせずに済む）。
       - `monitorGetPreviewSrc`：`monitorFileBlobs/{date}`を`once("value")`で個別取得し、
         data URL文字列をそのまま`<img>`/`<iframe>`の`src`に使う（Blob変換・
         `URL.createObjectURL`は不要。IndexedDBフォールバック時のみ従来通りBlobから
         オブジェクトURLを作る）。
       - Realtime Databaseの無料枠（1GB保存等）を圧迫しすぎないよう、
         `MONITOR_FILE_MAX_BYTES`（8MB）でファイルサイズを制限し、超過時は
         `monitorSaveFile`が分かりやすいエラーメッセージを投げるようにした。
       - 書き込み自体が（ルール未設定などで）応答無しのままになるケースに備え、
         `withTimeout(promise, ms, message)`という汎用ヘルパーは維持し、
         Realtime Databaseへの保存・読み込みにも15秒のタイムアウトを適用している。
    - **もう1つの重要な落とし穴**：`monitorFilesMeta`/`monitorFileBlobs`は今回新設した
      Realtime Databaseのパスだが、既存のルール（`energyData`/`settings`/`notes`にしか
      `.read`/`.write`を許可していなかった）はこれらの新パスをカバーしておらず、実際に
      読み書きしようとすると`permission_denied`エラーになることを動作確認で発見した
      （Firebase RTDBのルールはパスごとに明示的に許可されていないと拒否される。親を許可しても
      子が自動的に許可されるわけではなく、逆に新しい"きょうだいパス"を足しても既存ルールには
      含まれない）。**README.mdのルール例を`monitorFilesMeta`/`monitorFileBlobs`込みに
      更新済みだが、ユーザーが実際にFirebaseコンソール側のルールを更新するまでは、
      この機能はFirebase同期が有効な状態でも`permission_denied`で動作しない。** ルール更新は
      「Firebaseの初回セットアップ」手順4のJSONを参照してコンソールの「ルール」タブに
      再度貼り直してもらう必要がある（本ドキュメント作成時点でまだ未実施、動作未確認）。
26. **分析ビューの各グラフに「自動分析コメント」と、一部グラフに日/月/年の粒度切替を追加**。
    - **AI分析コメント**：「AIで分析したコメントを表示してほしい」との要望に対し、実際のLLM
      呼び出しではなく、**既存の数値（傾き・最大最小・前年比等）からJSで自動生成するルール
      ベースの短文コメント**を選択（ユーザーに確認済み。本物のLLM APIを呼ぶには
      Cloud Functions等のバックエンドが必要で、Firebase Storageと同様Blazeプラン必須になる
      可能性が高く、継続課金も発生するため）。
      - `trendInsight(labels, values, unit, digits)`という共通ヘルパー（先頭→末尾の変化率、
        線形回帰の傾きから「上昇/低下/横ばい」を判定、最大・最小の日/月/年も添える）を新設し、
        原単位トレンド・CO2トレンド・デマンド分析に適用。
      - 号機別稼働バランス・目標比較・ヒートマップ・複数年度重ね表示・気温感応度分析・
        曜日別パターン・累積進捗の残り8パネルは、それぞれのデータ構造に合わせた専用のコメント
        （稼働シェア最大号機、目標ペース比較、最大値の日付、年度間比較、感応度の経年変化、
        曜日別の最大最小と平日/休日比、累積進捗のペース比較等）を個別に実装。
      - 表示は`.analysis-note.ai-comment`（💬アイコン付き、薄い背景色のボックス）で統一。
      - 目標比較・累積進捗は、年度目標が未設定の場合コメント自体を出さない（対象データが
        無いため）。
    - **グラフごとの日/月/年切替**：11パネル全部には適用せず、明確に時系列トレンドとして
      意味を持つ**原単位トレンド・CO2排出量トレンド・デマンド分析の3つに限定**した
      （号機別稼働バランスはドーナツ、ヒートマップ・目標比較・累積進捗は年度内固定の構造、
      気温感応度分析は既に年度別バー、曜日別パターンは曜日軸固定——という理由でこれらには
      日/月/年の概念が素直に当てはまらないため、対象を絞ったとユーザーに説明済み）。
      - `granularityData(gran)`という共通関数を新設：`"day"`＝選択年度内の日次、
        `"month"`＝選択年度内の月次（既存デフォルト）、`"year"`＝全年度を1年度1点として集計、
        の3種類の`{labels, buckets}`を返す。各パネルはこれを`useMemo`で受け取り、
        `aggMetric`（原単位・デマンドは平均/合計をMETRICSの`kind`で自動判定）で系列を作る。
      - CO2トレンドは月次のときだけ前年度比較の第2系列を出し、日次・年次では単一系列にする
        （日次の「前年度比較」は日付の対応がずれるため、年次は既に全年度が1本の系列に
        含まれるため、という理由）。
      - `GranularityToggle`という共通の日/月/年ボタン群コンポーネントを新設し、3パネルの
        `panel-title-row`内に配置。

27. **分析ビューのグラフに「🤖 Geminiで分析」ボタンを追加（実際のGemini AI連携、任意機能）**。
    - 経緯：既存のルールベース自動コメント（項目26）とは別に、ユーザーから「本物のAIで分析
      してほしい」との要望があり、Firebase Storage・Firebase AI Logic（Vertex AI in Firebase）・
      Claude API直接呼び出しを順に検討したが、いずれも「新規プロジェクトでは無料プランで
      使えない／APIキーを公開リポジトリに置けない／ブラウザから直接呼べずバックエンドが要る」
      という壁にぶつかった。最終的に**Google Apps Scriptを無料の簡易バックエンドとして使う**
      構成に落ち着いた（ユーザー自身の個人Googleアカウントで試す前提）。
    - **アーキテクチャ**：`app.jsx`は`GEMINI_PROXY_URL`（Apps Scriptのウェブアプリ公開URL）に
      対してのみ`fetch`する。実際のGemini APIキーは一切このリポジトリに含まれず、
      Apps Script側の「スクリプト プロパティ」にのみ保管される
      （`scripts/gemini-proxy-apps-script.gs.txt`がそのApps Script用のコード。
      energy-dashboard-app本体の実行には使われない、コピー用の参考ファイル）。
    - **CORS回避の実装ポイント**：`fetch`で`Content-Type`ヘッダーを明示的に指定すると
      ブラウザがプリフライト(OPTIONS)リクエストを送るが、Apps ScriptのWebアプリは
      OPTIONSに正しく応答しないため失敗する。`body: JSON.stringify(...)`だけを渡し
      `Content-Type`ヘッダーを指定しない（ブラウザ既定の`text/plain`のまま送る）ことで
      プリフライトを回避し、Apps Script側で`e.postData.contents`をパースする、という
      構成にしている。
    - `fetchGeminiComment(prompt)`・`buildAnalysisPrompt(title, dataText)`・
      `seriesText(labels, values, unit)`という3つの共通関数と、`AiCommentBox`という
      共通コンポーネント（ボタン+ローディング状態+結果表示+エラー表示）を新設し、
      分析ビューの11パネル（4番目「目標比較」と11番目「累積進捗」は年度目標が設定されて
      いない場合は非表示）それぞれに、そのパネルの実データを要約したプロンプトを渡して
      配置した。
    - **未設定時（`GEMINI_PROXY_URL`がプレースホルダーのまま）は、ボタンを押すと
      分かりやすいエラーメッセージが表示されるだけ**で、他の機能には一切影響しない設計。
      README.mdに詳しいセットアップ手順（AI StudioでのAPIキー発行→Apps Scriptへの
      貼り付け→スクリプトプロパティ登録→ウェブアプリとしてデプロイ→発行されたURLを
      `GEMINI_PROXY_URL`に設定）を記載した。
    - **デバッグ時に発覚した罠**：この機能を実装した直後、ブラウザで新しく追加した
      `AiCommentBox`が全く描画されない（コンソールエラーも無し）という不可解な状態に
      遭遇した。`app.compiled.js`をfetchして中身を確認したところ、ビルド直後のはずなのに
      `AiCommentBox`の文字列すら含まれておらず、ディスク上のファイル（`grep`で確認済み、
      正しく最新化されていた）と食い違っていた。原因は**プレビュー用の開発サーバ
      （`scripts/dev-static-server.js`）の古いプロセスが、`preview_list`（プロセス一覧）にも
      OSレベルのポート一覧にも出てこない状態のまま、なぜかポート5183で応答し続けていた**こと
      （ゾンビプロセスのようなもの）。`preview_start`を呼び直して新しいプロセスとして
      明示的に起動し直したところ解決した。**今後「コードを直したのに反映されない」という
      症状が起きたら、まずキャッシュを疑う前に、プレビューサーバを一度完全に再起動
      （`preview_stop`→`preview_start`、または新規タブでの確認）してみること。**

28. **Gemini連携（項目27）のモデル名選定で、実際に本番のApps Scriptにデプロイして初めて
    分かった無料枠の実情を記録**。
    - 当初`gemini-2.0-flash`を指定 → `Quota exceeded ... limit: 0`（このAPIキーでは無料枠が
      0で使用不可）。
    - `gemini-1.5-flash`に変更 → `models/gemini-1.5-flash is not found`（世代として廃止済み、
      モデル一覧に存在しない）。
    - `gemini-2.5-flash`に変更 → `This model ... is no longer available to new users.`
      （新規ユーザーには提供終了）。
    - 最終的に、常に現行の推奨モデルを指すエイリアス**`gemini-flash-latest`**に変更して解決。
      実際にGeminiによる分析コメントが生成されることを確認済み。
    - **教訓**：Gemini APIは無料枠・提供状況がモデル世代ごと・アカウント作成時期ごとに
      大きく変わるため、モデル名を当てずっぽうで決め打ちしないこと。
      `scripts/gemini-proxy-apps-script.gs.txt`に`listModels()`という一時実行用関数を
      追加してあり、これを実行してApps Scriptの実行ログを見れば、そのAPIキーで実際に
      `generateContent`に対応しているモデル名の一覧が取得できる。次回同様のエラーが
      出た場合は、まずこれで確認すること。
    - 現状のデフォルトは`var model = "gemini-flash-latest";`
      （`scripts/gemini-proxy-apps-script.gs.txt`内、本番の実際のApps Scriptプロジェクトにも
      同じ変更を反映済み・デプロイ済み＝バージョン5、URLは変更なし）。

29. **ダッシュボード画面にも「🤖 Geminiで分析」ボタンを追加**。
    - 分析ビュー（項目27）は固定パネルごとの分析だったが、ダッシュボードのKPI/メイングラフ
      セクションにも同様のボタンを設置。表示中のタブ（`view`state）が「月」（`view==="day"`）
      なら月次実績、「年」（`view==="month"`）なら年度実績として、KPIカードの数値・前期比・
      総合熱源効率・CO2排出量・概算コスト・電力ピークを要約したプロンプトを渡す。
      `kpi-row`セクション内、`combined-chart-wrap`の直後に配置（`app.jsx`内`<AiCommentBox>`）。

30. **Gemini分析に「おすすめグラフの組み合わせ提案＋ワンクリック追加」機能を実装**。
    - 背景：ユーザーから「Geminiにグラフも作ってもらえるか」と聞かれたが、画像生成AIに実データを
      正確に描かせるのは不正確になりがちなため非推奨と回答。代わりに「Geminiがどの項目を
      組み合わせるべきか提案し、実際のグラフ描画は既存のChart.jsに任せる」設計を提案し、
      承諾を得て実装。
    - `buildAnalysisPrompt(title, dataText, extraInstruction)`に第3引数を追加（既存の11パネルは
      未指定のままで影響なし）。ダッシュボード用の呼び出しのみ`buildChartSuggestionInstruction()`
      （`METRICS`の全キー一覧を渡し、回答の最後の1行だけに`SUGGEST_CHART: キー1,キー2`という
      機械可読形式で出力するようGeminiに指示する）を渡す。
    - `parseChartSuggestion(rawText)`で、Geminiの回答から末尾の`SUGGEST_CHART:`行を正規表現で
      抜き出し、本文コメントと提案キー配列に分離。
    - `AiCommentBox`に`onAddChart`という省略可能なpropを追加。渡されている場合のみ
      `parseChartSuggestion`を実行し、提案があれば「📊 おすすめグラフの組み合わせ: ○○×△△」＋
      「＋このグラフを追加」ボタンを表示。押すと`onAddChart(keys, name)`を呼ぶ。
      渡されていない（＝分析ビューの11パネル）場合は従来通り生のコメントをそのまま表示するだけで、
      挙動は変わらない。
    - `App`内に`addChartWithMetrics(keys, name)`を新設（既存の`addChart()`と同様だが、
      デフォルトの1項目ではなくGemini提案のキー配列で`selectedMetrics`を初期化し、
      グラフ設定モーダルは開かない）。ダッシュボードの`AiCommentBox`に
      `onAddChart={addChartWithMetrics}`として渡している。
    - 実機（ブラウザプレビュー）で「熱負荷(合計)×重油使用量(合計)×総合熱源効率」という提案が
      実際に返ってきて、「＋このグラフを追加」を押すとその3項目が選択された新しいグラフパネルが
      画面下部に追加されることを確認済み。

31. **中央監視画面取り込み（項目24・25）に「🤖 Geminiで画像分析」ボタンを追加**（画像/PDFの
    マルチモーダル分析）。
    - `monitorGetFileDataUrl(dateStr)`を新設：表示用の`monitorGetPreviewSrc`は同期時はdata URL、
      IndexedDBローカル時はオブジェクトURL（blob:）を返すため、そのままではGeminiに渡す
      base64データとして使えない。この関数は表示方式によらず常にbase64のdata URL文字列を
      返すようにしている（IndexedDB利用時は`fileToDataUrl`でBlobから変換）。
    - `fetchGeminiImageComment(prompt, dataUrl, mimeType)`を新設。data URLの`,`以降（base64本体）
      とMIMEタイプをApps Scriptへ送る。テキストのみの`fetchGeminiComment`とは別関数。
    - `MonitorPreviewModal`に「🤖 Geminiで画像分析」ボタンを追加。押すと、その日付のファイルを
      `monitorGetFileDataUrl`で取得し、「中央監視装置の熱源画面を読み取り、注意点や異常の兆候が
      あれば指摘して」という趣旨のプロンプトと合わせて送信する。PDF（`application/pdf`）も
      画像と同じ経路で送信している（Geminiのドキュメント理解機能に依存）。
    - **重要：Apps Script側の対応も必要**。`scripts/gemini-proxy-apps-script.gs.txt`の`doPost`を、
      `body.imageBase64`が送られてきた場合に`inlineData: {mimeType, data}`パーツを
      `contents[0].parts`に追加するよう更新した。**ただし本番の実際にデプロイ済みのApps Script
      プロジェクトはまだこの変更が反映されていない**（ローカルの参考ファイルのみ更新・コミット
      済み）。ユーザーには、script.google.comで該当プロジェクトのdoPostを書き換えて
      「デプロイを管理」→新バージョンでデプロイし直してもらう必要がある、と伝えること。
    - 動作確認：ブラウザプレビューでドラッグ&ドロップの代わりにJSでDataTransferを使い1x1pxの
      テストPNGを疑似アップロードし、プレビューモーダルから「🤖 Geminiで画像分析」を押して
      実際にGeminiまで通信が届くことを確認（本番Apps Script未更新のため「画像が添付されていない」
      という想定通りの返答だった＝配線は正常）。テスト後、このテストファイルは実際のユーザーの
      Firebaseプロジェクトから削除済み。

32. **中央監視画面取り込みモーダルに「📷 カメラで撮影」ボタンを追加**（iPadでその場で撮影した
    写真を直接取り込みたいという要望）。
    - `capture="environment"`（背面カメラ指定）を付けた2つ目の`<input type="file" accept="image/*">`
      を用意し、既存のドラッグ&ドロップ/ファイル選択用inputとは別に配置。
    - 通常のファイル選択input（`MONITOR_FILE_ACCEPT`＝PDF/JPEG/PNG）はiOS/iPadOSでは
      タップすると「写真ライブラリ」「ファイルを選択」なども選択肢に出るが、確実に一発で
      カメラを起動させたいというユーザーの要望に応えるため、`capture`属性付きの専用ボタンを
      別途設けた（PC/Androidなどカメラの無い/挙動が異なる環境でも、単に画像ファイル用の
      input扱いになるだけで害はない）。
    - ブラウザプレビュー（Chromiumベース、実カメラなし）では`accept="image/*"`・
      `capture="environment"`が正しく設定されていることをDOM上で確認済み。実機のiPadでの
      実際のカメラ起動そのものはこの自動テスト環境では検証できないため、ユーザーに確認を依頼。

33. **中央監視画面を1日に複数枚登録できるようにし、画像自動圧縮・画面ごとのGemini分析・
    全画面総括を実装**（1日に複数のエネルギー監視画面を扱いたいという要望、および
    「複数枚にした場合の保存容量」の質問への回答として、圧縮を前提に実装した）。
    - **経緯**：ユーザーから「監視画面が複数ある場合、まとめて分析すべきか1枚ずつ分析すべきか」
      と聞かれ、精度重視で「1枚ずつ個別分析→複数の結果をまとめて総括」の2段階方式を提案し
      承諾を得た。続けて「複数枚にした場合の保存容量は？」との質問に対し、iPadカメラ画像
      （2〜8MB程度）を無圧縮でRealtime Database（無料枠1GB、Base64で約1.33倍に膨張）に
      複数枚×毎日保存すると数ヶ月〜1年で枯渇する試算を示し、対策として「アップロード時の
      自動圧縮・リサイズ」を提案。ユーザーが「自動圧縮リサイズした複数枚対応、各画面gemini分析、
      geminiで総括」を実装するよう明示的に依頼。
    - **データ構造の変更**：`monitorFilesMeta/{date}`・`monitorFileBlobs/{date}`が、
      1ファイル分のオブジェクト/文字列を直接持つフラットな形式だったのを、
      `monitorFilesMeta/{date}/{fileId}`・`monitorFileBlobs/{date}/{fileId}`という
      ネスト形式に変更。fileIdは`genMonitorFileId()`（`Date.now().toString(36) + Math.random()...`）
      で生成するクライアント側の簡易ID。Firebaseのセキュリティルールは`monitorFilesMeta`/
      `monitorFileBlobs`のパス丸ごとに`.read`/`.write: true`を設定しているだけなので、
      ネストが1段増えてもルールの変更・再設定は不要だった。
    - **旧データとの互換**：App本体（`app.jsx`内の`monitorFilesMeta`購読ハンドラ）は、
      値が`{fileName: ...}`のような旧形式（フラットな1ファイル分のオブジェクト）ならば
      `fileId: "legacy"`として読み替え、新形式（`{fileId: {fileName: ...}, ...}`）なら
      そのまま展開する、という両対応の読み取りロジックにしている。書き込みは常に新形式。
    - **画像圧縮**：`compressImageFile(file, maxDim, quality)`で、Canvas経由で長辺を
      `MONITOR_IMAGE_MAX_DIM`(1600px)まで縮小し、JPEG品質`MONITOR_IMAGE_QUALITY`(0.75)で
      再エンコードしてから保存する。PDFは圧縮できないためそのまま（サイズ上限8MBは従来通り）。
      ブラウザプレビューでの実機テストでは、8MB超のノイズ画像2枚（10.3MB/7.4MB）が
      878KB/793KBまで圧縮されることを確認済み。
    - **IndexedDB（Firebase未設定時のローカルのみフォールバック）も複数ファイル対応**：
      旧ストア`monitorFiles`（keyPath="date"）はそのまま残しつつ、新ストア
      `monitorFilesV2`（keyPath="id"＝`date + "__" + fileId`、`date`にインデックスあり）を
      追加し、`IDB_VERSION`を2→3に上げた。今後このフォールバックを実際に使うユーザーが
      現れた場合、旧ストアのデータは自動移行されない点に注意（そもそもこのユーザーは
      Firebase同期を使っているため影響なし）。
    - **UIの変更**：`MonitorImportModal`はドラッグ&ドロップ/ファイル選択に`multiple`属性を
      追加し、複数ファイルを一括受付→それぞれ圧縮状態（圧縮中/完了サイズ/エラー）を
      一覧表示→個別に「×」で除外可能、にした。カメラボタンは1回のタップで1枚のままだが、
      複数回タップすれば追加されていく。`MonitorPreviewModal`は指定日の全ファイルを
      カード形式で並べ、各カードに従来通りの個別分析ボタンを残しつつ、ファイルが2枚以上の
      場合のみ「🤖 Geminiで総括（全画面まとめて分析）」ボタンを追加。総括ボタンは、
      まだ個別分析していない（またはエラーだった）画面を先に`fetchGeminiImageComment`で
      分析し、全画面分のコメントを`buildMonitorSummaryPrompt`でまとめてテキストのみの
      `fetchGeminiComment`に渡して総括文を生成する2段階構成。
    - 実機確認：ブラウザプレビューで2枚同時ドラッグ&ドロップ→自動圧縮→2枚保存→
      一覧に2行表示→プレビューで2枚のカード表示→総括ボタン押下で個別分析2回＋総括1回、
      計3回のGemini API呼び出しが実際に発生することを確認（この時はGemini側が
      一時的な混雑エラーを返したが、通信の配線自体は正常に機能した）。確認用のテスト画像は
      実際のFirebaseプロジェクトから削除済み。

34. **実績が1件も無い期間（まだ来ていない未来の月・年度）でも、過去データから予測を
    出せるようにした**（ユーザーから「何も入力してなくても予測を出してほしい」との要望）。
    - **経緯**：ユーザーから提示されたスクリーンショットは実は「30/30日入力済みの完了済みの月」
      で、予測は「まだ入力されていない残り日数がある期間にだけ出る」という仕様上、
      表示されないのが正しい挙動だった（バグではないと回答済み）。ところが続けて
      「何も入力してなくても予測を出してほしい」と明示的に依頼されたため、これは
      従来の仕様を変更する新機能として対応した。
    - `computeMonthForecast`は元々`monthRows.length === 0`（その月の実績が0件）なら
      即`null`を返し、予測を一切出さない仕様だった。これを、`latestDate`（最新実績日）を
      新たに引数で受け取り、「対象月が最新実績日より後（＝まだ来ていない月）」の場合だけ
      実績0件でも予測を続行するよう変更。前年同月の1日あたり平均→それも無ければ全実績の
      平均、をpaceのフォールバックにした。**過去の月に実績が0件の場合は、データ欠落
      （3.2節と同じ問題）の可能性があるため、従来通り予測しない**方針は維持している。
    - `computeFiscalYearForecast`も同様に、対象年度が最新実績の年度より後（丸ごと未来の
      年度）の場合は`currentIdx`を`-1`にすることで、全12ヶ月を「未来」区分として一括予測
      対象にするよう変更。pace予測のフォールバックとして前年度全体の月平均を追加。
      過去の年度に実績が無い場合（データ欠落）は従来通り予測しない。
    - **この過程で実データの不具合を発見**：`historicalAvgTempForCalendarDay`
      （気温回帰予測で「過去の同じ月日の平均気温」を求める関数）が、実績0件の月を予測
      しようとした際にNaNを返す不具合があった。原因を調査したところ、実際のFirebase上の
      データで**`2025-11-12`のtempAvg（外気温度）が数値ではなく`".15.4"`という不正な
      文字列**になっており、これが平均計算に混ざると文字列結合されてNaNになっていた
      （他の集計関数は`!isNaN(v)`で数値以外を除外しているが、この関数だけ抜けていた）。
      `!isNaN(v)`のガードを追加して修正。**なお`2025-11-12`のtempAvgの実データ自体は
      不正な文字列".15.4"のままで、ユーザーには報告済み（憶測で自動修正はしていない。
      3.2節の方針と同じ）。ユーザーが希望すれば「データ入力」画面から手動で修正できる。**
    - 実機確認：Firebaseの実データに対し、実績0件の未来月（2026年6月・気温回帰予測）と
      実績0件の未来年度（2026年度・気温回帰予測）の両方で、Chart.jsの実際のデータセットを
      直接検査し、全期間にわたってNaN無しの数値が入っていることを確認済み。

35. **ダッシュボードのKPIカードに「比エンタルピー(外気)」を追加**（ユーザーからの
    直接的な機能追加依頼）。
    - `calcSpecificEnthalpy(tempC, rhPct)`を新設。Tetens近似式で飽和水蒸気圧を求め、
      標準大気圧(1013.25hPa)を前提に湿り空気の比エンタルピー(kJ/kg(DA))を計算する。
      設定（`settings`）に依存しないため、`withComputed`ではなく`withDerived`（データ読込時に
      毎回適用される、settings非依存の派生値を計算する関数）内で計算するようにした。
    - `METRICS`に`enthalpyKJkg`（気象グループ、`kind: "avg"`、色`#0ea5e9`）として登録。
      これにより既存のグラフ設定（`MetricSelector`）からも自動的に選択可能になる。
    - ダッシュボードのKPIカード列に、他の主要KPI（電力量・重油使用量など）と同様に
      前期比較付きのカードとして追加。ただし`kind: "avg"`のため、既存の`kpiList`（`sumMetric`
      前提の4項目固定リスト）には含めず、`efficiency`と同様に専用の`enthalpyStat`
      useMemoを新設して`avgMetric`で集計している（合計してはいけない値を誤って
      合計しないための設計）。
    - 動作確認：25℃/50%RHで約50.3kJ/kg、35℃/60%RHで約90.1kJ/kgなど、湿り空気線図の
      既知の参照値と計算結果が一致することをブラウザ上で確認済み。実データ（2026年3月、
      平均気温・湿度から算出）でも前期比較付きで正しく表示されることを確認。

36. **分析ビューに「12. 日付比較分析（前日・前週・前月・前年）」パネルを追加**
    （ユーザーからの直接的な機能追加依頼：カレンダーで基準日を選び、ダッシュボードと
    同じ項目で前日/前週/前月/前年と比較したい、という要望）。
    - `DASHBOARD_KPI_KEYS`定数（ダッシュボードのKPIカードと同じ項目：受電電力量・
      重油使用量・冷水/温水負荷・デマンド・総合熱源効率・CO2排出量・原油換算値・概算コスト・
      比エンタルピー）を新設し、比較対象をここから生成している。
    - `addMonthsClamped(dateStr, monthsDelta)`を新設。前月・前年の日付計算で、対象月に
      同じ日が無い場合（例：3/31の前月＝2/31は存在しない）は、その月の末日にクランプする
      （`daysInMonth`を利用）。
    - `input type="date"`（ネイティブのカレンダーピッカーが出る）で基準日を選ぶと、
      `dateCompare` useMemoが前日(`addDays(-1)`)・前週(`addDays(-7)`)・前月・前年
      それぞれの日付の同項目の値と前期比%を計算し、表形式（`entry-list-table`の
      スタイルを再利用）で表示する。データが存在しない日付（欠測日）は「データなし」と
      表示され、憶測で埋めることはしない。
    - この表の内容を要約したプロンプトでGeminiに分析させる`AiCommentBox`も設置済み。
    - 動作確認：ブラウザプレビューで基準日を2026-03-31→2025-08-15に変更し、
      前日/前週/前月/前年の日付と数値が正しく再計算されることを確認済み。

37. **熱源機器の定格能力を設定に追加し、「設備稼働率」KPIを実装**（ユーザーから
    「竣工図・機器リストに能力（kW）の記載がある、分析に活かせないか」との相談）。
    - 冷温水発生器No.1/No.2・ボイラーNo.1/No.2、計4台それぞれの定格能力(kW)を
      `settings`に追加（`chiller1CapacityKW`等）。**ユーザーの了承のもと、現時点では
      架空の仮数値（500/500/300/300kW）をデフォルトにしている。実際の値は竣工図・
      機器リストを見ながらユーザー自身が設定画面から入力する想定で、まだ入力されていない。**
      設定画面には「⚠現在は仮の数値です」という注意書きを明記した。
    - 4台の定格能力の合計から「1日あたりの理論上限熱量(GJ)」を算出し
      （`totalRatedCapacityKW × 24h × 0.0036(kWh→GJ)`）、実際の負荷熱量
      （冷水負荷+温水負荷）との比率を「設備稼働率」としてKPI化。デマンドの
      「契約電力比」と同じ発想で、設備容量に対する使用率を示す指標。
    - `METRICS`に`equipUtilPct`として登録（`withComputed`で日次算出）、
      `DASHBOARD_KPI_KEYS`にも追加したため、グラフ選択・日付比較分析パネル
      （項目36）からも自動的に利用できる。
    - 既存ユーザーの`settings`（localStorage/Firebaseどちらも）は
      `{...DEFAULT_SETTINGS, ...savedSettings}`という既存のマージパターンのおかげで、
      新フィールドが無くても自動的に仮数値が補われる。移行作業は不要だった。
    - **今後の会話で「定格能力を実際の値に直したい」と言われたら、設定画面の
      「熱源機器の定格能力」セクションで4つの数値を編集するだけでよい**（コード変更は不要）。

38. **設定画面に「機器表(PDF/画像)から自動入力」ボタンを追加**（項目37の定格能力を
    手入力する代わりに、竣工図・機器リストをアップロードしてGeminiに読み取らせたい
    という要望）。
    - 中央監視画面機能（項目31）で作った`compressImageFile`/`fileToDataUrl`/
      `fetchGeminiImageComment`をそのまま再利用し、新しい保存先は作っていない
      （このアップロードはFirebaseやIndexedDBに保存されず、その場でGeminiに送って
      結果をフォームに反映するだけの一時的な処理）。
    - `buildEquipCapacityExtractionPrompt()`で「冷温水発生器No.1/No.2・ボイラーNo.1/No.2の
      定格能力(kW)を読み取り、USRt/kcal/h等の単位で書かれていてもkWに換算し、
      回答の最後の行にだけ`EXTRACTED_CAPACITY: {"chiller1":...,"chiller2":...,"boiler1":...,
      "boiler2":...}`というJSON形式で出力する」よう指示。`parseExtractedCapacity()`で
      その行を正規表現抜き出し→`JSON.parse`する（項目30のSUGGEST_CHART行と同じ
      「回答末尾に機械可読な1行を仕込む」パターン）。
    - 読み取れた項目だけを`SettingsModal`内の`form`state（まだ「保存」されていない
      ローカルなドラフト）に反映する設計。**「保存」を押すまで実際の設定には反映されない**
      ため、誤読があってもユーザーが確認・修正してから確定できる（憶測データを勝手に
      確定しない、というプロジェクト全体の方針と一致）。
    - 動作確認：定格能力を明記したテスト画像（880/880/350/350kW）をブラウザ上で
      生成し、Geminiの読み取り→フォームへの反映まで正しく動作することを確認済み
      （テストなので保存はせず、実際の設定は仮数値500/500/300/300のまま維持）。

39. **中央監視画面の「瞬間出力」を読み取り、最適運転アドバイスをGeminiに提案させる
    機能を追加**（項目38に続く一連の相談の着地点）。
    - 経緯：「複数の熱源機器がある場合、どの組み合わせが効率的か分析できるか」
      →「号機ごとの出力（熱量）の記録があれば可能か」→「中央監視画面には各機器の
      熱量が表示している、読み込み→分析→最適運転の提案はできるか」→ユーザーへの
      確認の結果、**画面に表示されているのは瞬間出力（その場の値）であり、蓄積した
      履歴ではなく「今この瞬間、どう運転するのが効率的か」という単発のアドバイスが
      欲しい**、という要望に絞られた。号機別の熱量を継続的に蓄積して比較する
      （項目36の延長）案は、瞬時値のみでは日次の効率比較に使えないため見送った。
    - `buildOptimalOperationPrompt(dateStr, fileName, settings)`を新設。
      設定画面で入力済みの熱源機器4台の定格能力(kW)（項目37）をプロンプト内に
      テキストとして埋め込み、Geminiに「画面から各機器の現在出力を読み取り、
      定格能力に対する負荷率(%)を算出した上で、部分負荷運転による効率低下を
      踏まえた運転改善アドバイスを300字程度で述べる」よう依頼する。JSON抽出は
      不要で、そのまま人間が読むテキストとして表示する（項目30・38の機械可読な
      1行抽出パターンとは異なり、最終出力自体がアドバイス文なのでシンプル）。
    - `MonitorPreviewModal`の各ファイルカードに「🤖 最適運転アドバイス」ボタンを追加
      （既存の「🤖 Geminiで画像分析」の下）。定格能力を使うため、`settings`を
      `App`→`DataEntryView`→`MonitorPreviewModal`とpropsで新たに渡すよう変更した。
    - 動作確認：各機器の瞬間出力を明記したテスト画像（冷温水発生器No.1=80kW,
      No.2=0kW, ボイラーNo.1=20kW, No.2=0kW）をアップロードし、実際にGeminiが
      定格能力（仮数値500/500/300/300kW）に対する負荷率を正しく算出
      （16.0%/0%/6.7%/0%）した上で、「低負荷運転が続いている、冷温水発生器への
      負荷一本化・蓄熱槽活用・間欠運転への切替」といった具体的な改善提案を
      生成することを確認済み。テストデータは削除済み。

40. **中央監視画面取り込みに画像プレビューと「保存のみ/保存してGemini分析」の
    選択を追加**（ユーザーから、実際にサンプル画像で試した流れで「取り込んだら
    プレビュー画面を表示し、"この画像で保存しますか"→保存のみ/保存してGemini分析、
    という選択を追加してほしい」との依頼）。
    - `MonitorImportModal`の選択ファイル一覧を、ファイル名だけのテキストリスト
      （`.monitor-selected-list`）から、実際のサムネイル画像（`URL.createObjectURL`、
      画像は`<img>`、PDFは`<iframe>`）付きのカード一覧（`.monitor-preview-list`）に
      変更し、「この画像で保存しますか？」という確認文言を追加した。
    - 保存ボタンを2つに分割：「保存のみ」（従来通りモーダルを開いたまま完了メッセージを
      表示）と「🤖 保存してGeminiで分析」（保存後に取り込みモーダルを自動的に閉じ、
      その日の`MonitorPreviewModal`——Geminiで画像分析・最適運転アドバイス・総括の
      ボタンがある画面——を自動的に開く）。
    - **アーキテクチャ変更**：この「保存後に別画面（プレビュー）を自動的に開く」という
      動線を成立させるため、`previewMonitorDate`/`previewMonitorFiles`のstateと
      `MonitorPreviewModal`の描画を、`DataEntryView`内のローカルstateから`App`直下に
      引き上げた。理由：中央監視画面の取り込みボタン（ヘッダー）は`App`直下にあり
      どの画面からでも開けるが、プレビューはこれまで`DataEntryView`の中でしか
      開けなかったため、画面をまたいだ遷移ができなかった。`DataEntryView`は
      `onOpenMonitorPreview`というコールバックpropを呼ぶだけのシンプルな形に変更し、
      不要になった`settings`propの受け渡しも削除した（`MonitorPreviewModal`は
      `App`から直接`settings`を受け取るようになったため）。
    - 動作確認：ブラウザプレビューで画像をドロップ→サムネイルが実際に表示される
      ことを確認→「保存してGeminiで分析」を押すと、取り込みモーダルが閉じて
      その日のプレビュー画面が自動的に開くことを確認済み。テスト画像は削除済み。

41. **カメラ複数枚撮影の導線改善と、複数画像まとめてのGemini最適運転アドバイスを追加**
    （ユーザーから「カメラで複数枚撮影して取り込みたい。1枚目撮影時に"続けてカメラ撮影する"
    ボタンを追加してほしい。Gemini分析は複数枚まとめてお願いしたい」との依頼）。
    - **カメラボタンのラベル切替**：`MonitorImportModal`のカメラボタンは元々何度でもタップ
      すれば複数枚追加できる仕様だったが、それが伝わりにくかったため、`selectedFiles.length > 0`
      のときはラベルを「📷 カメラで撮影」→「📷 続けてカメラ撮影する」に切り替えるようにした
      （別ボタンを新設するのではなく、同じボタンのラベルを動的に変える形で対応）。
    - **複数画像を1回のGemini呼び出しでまとめて渡す仕組み**：これまでの「🤖 Geminiで総括」
      （項目30）は「各画像を個別にGeminiへ送って得たテキストの要約」を後からまとめる
      2段階方式だったが、今回追加した「🤖 最適運転アドバイス（全画面まとめて）」は
      **複数の画像そのものを1回のAPI呼び出しに同梱**する方式（`fetchGeminiMultiImageComment`、
      `body.images`に`[{mimeType, data}, ...]`の配列を渡す）。監視画面が1台のカメラでは
      収まらず複数枚に分けて撮影する必要がある場合、画面をまたいだ機器も含めて横断的に
      Geminiに見比べてもらえる。`buildOptimalOperationMultiPrompt()`で構築。
      `MonitorPreviewModal`のファイルが2枚以上の場合のみボタン表示。
    - **Apps Script側の対応も必要**：`scripts/gemini-proxy-apps-script.gs.txt`の`doPost`を、
      `body.images`の配列が送られてきた場合は複数の`inlineData`パーツとして追加するよう
      更新した。既存の単数`imageBase64`との後方互換は維持。**本番の実際にデプロイ済みの
      Apps Scriptプロジェクトはまだこの変更が反映されていない**ため、ユーザーには
      script.google.comで該当プロジェクトのdoPostを書き換えて「デプロイを管理」→
      新バージョンでデプロイし直してもらう必要がある、と伝えること（項目27・31と同じ手順）。
    - 動作確認：ブラウザプレビューでカメラを模した2回の撮影→ラベルが正しく切り替わる
      ことを確認→「保存してGeminiで分析」→3枚（既存1枚+新規2枚）のプレビュー→
      「最適運転アドバイス（全画面まとめて）」ボタン押下、まで一連の配線が正常に動作
      することを確認（本番Apps Script未更新のため「画像が添付されていない」という
      想定通りの返答だった＝配線は正常）。テストデータは削除済み。

42. **中央監視画面取り込みモーダルにカレンダーと既存登録プレビューを追加**（ユーザーから
    「取り込み画面にカレンダーとプレビューを表示してほしい」との依頼）。
    - `MonitorImportModal`の「対象日」入力（`<input type="date">`）の下に、月表示の
      コンパクトなカレンダー（前月/翌月ナビゲーション、`daysInMonth`を利用した日数計算は
      `DataEntryView`のカレンダーと同じロジック）を追加。既に監視画面が登録済みの日には
      🖥マークを表示し、日付入力とカレンダーのセルクリックの両方から日付を選べるように
      した（`pickDate()`で両方を同期）。
    - 選択中の日付に既存登録があれば、`monitorGetPreviewSrc`でサムネイルを取得して
      「この日にはすでにN件登録されています」という一覧を表示。「この日をまとめて開く
      （Geminiで分析など）」ボタンから、取り込みモーダルを閉じて既存の
      `MonitorPreviewModal`（項目40でApp直下に引き上げ済み）へ直接遷移できる。
    - `App`→`MonitorImportModal`に`monitorFiles`・`onOpenExistingPreview`（実体は
      `setPreviewMonitorDate`）を新たにpropsで渡すよう変更。
    - 動作確認：ブラウザプレビューでカレンダーに🖥マークと既存ファイルのサムネイルが
      正しく表示され、別の日をクリックすると日付入力欄が連動し、既存登録なしの日では
      プレビュー欄が消えることを確認済み。

43. **中央監視画面取り込みをポップアップからヘッダー下のインライン画面に変更、UI細部を調整**
    （項目42のスクリーンショットを見たユーザーから、複数の具体的なフィードバック）。
    - **ポップアップ→インライン表示**：`MonitorImportModal`を`modal-backdrop`/`modal`の
      ポップアップから、`.monitor-import-panel`というダッシュボード等と同じmain content
      領域に表示するインラインパネルに変更。`App`側で`showMonitorImport`がtrueの間は
      既存の`mode`（dashboard/analysis/entry）の表示を丸ごと隠してこのパネルに差し替え、
      「閉じる」を押すと元の`mode`の画面に戻る（`mode`状態自体は変更していないので、
      分析画面を見ていたならそこに、データ入力画面を見ていたならそこに戻る）。
    - **サムネイルクリックでファイルを開く**：既存登録ファイルのサムネイルに
      `onClick={() => onOpenExistingPreview(date)}`を追加し、クリックで
      `MonitorPreviewModal`が開くようにした。これにより冗長だった
      「この日をまとめて開く」ボタンは削除。
    - **「保存してGemini分析」後もパネルを閉じない**：`handleUpload`のandAnalyze分岐から
      `onClose()`呼び出しを削除。取り込みパネル（カレンダー）はプレビューモーダルの
      裏にそのまま残り続けるため、プレビュー側の「閉じる ✕」を押すと自然にカレンダー
      画面に戻る（ユーザー要望「プレビューを閉じてもカレンダー表示に戻るように」に対応）。
    - **ダウンロードボタンの右端揃え**：`MonitorPreviewModal`の`.monitor-file-card-header`は
      `justify-content:space-between`で3要素（タイトル・ダウンロード・削除）を配置していた
      ため、ダウンロードボタンが中央寄りになっていた。ダウンロード+削除を
      `.monitor-file-card-actions`という1つのグループにまとめ、2要素配置にすることで
      右端揃えにした。
    - **画像分析＋最適運転アドバイスのボタン統合**：個別だった「🤖 Geminiで画像分析」
      「🤖 最適運転アドバイス」の2ボタンを「🤖 Geminiで分析」1つに統合し、クリックで
      `analyzeOneFile(f)`と`analyzeOptimalOperation(f)`を同時に実行するようにした。
      結果のコメントは従来通り2つとも別々の段落で表示（🤖=画像分析、💡=運転アドバイス、
      という絵文字での区別は維持）。
    - 動作確認：ブラウザプレビューで、取り込みパネルがポップアップでなくインライン表示
      されること、サムネイルクリックでプレビューが開くこと（裏の取り込みパネルは
      維持されたまま）、プレビューの「閉じる」でカレンダーに戻ること、ダウンロード/削除
      ボタンが右端（差分0px）に揃うこと、統一した「🤖 Geminiで分析」ボタンで画像分析・
      運転アドバイス両方のコメントが表示されることをすべて確認済み。

44. **中央監視画面取り込みパネルを整理：プレビューステップ分離・カレンダー縮小・
    保存日リスト追加**（項目43のスクリーンショットを見た続きのフィードバック）。
    - **プレビューステップの分離**：`selectedFiles.length > 0`になった瞬間、カレンダー・
      「画面を保存した日」一覧・既存登録プレビューを非表示にし、代わりに
      「この画像で保存しますか？」+サムネイル一覧+保存ボタン（保存のみ/保存してGemini
      で分析）だけの`.monitor-import-preview-step`を表示するようにした。選択したファイルを
      全部「×削除」すると自動的にカレンダー表示へ戻る（明示的な「戻る」ボタンは設けず、
      selectedFilesの有無だけで自動的に画面が切り替わる設計）。ドロップゾーンとカメラ
      ボタンはどちらの状態でも常に表示し、プレビュー中でも追加のファイルを取り込める。
    - **カレンダー縮小**：画面幅いっぱいに広がっていたカレンダーを、幅240px・セル高さ26px
      に縮小（`aspect-ratio:1`をやめて固定高さに変更、フォントサイズも11px前後に縮小）。
    - **「画面を保存した日」一覧を追加**：カレンダーの下に`savedDatesList`
      （`monitorFiles`から日付ごとの件数を集計し新しい順にソート）を一覧表示。
      クリックでその日にジャンプできる（`pickDate`を再利用）。月をまたいだ日付にも
      カレンダーをページ送りせずすぐアクセスできるようにする狙い。
    - 動作確認：ブラウザプレビューでファイル選択時にカレンダーが隠れてプレビュー
      ステップに切り替わること、カレンダーが240×202pxに縮小されたこと、保存後に
      「画面を保存した日」リストが実際に更新される（🖥マーク・既存プレビューとも連動）
      ことを確認済み。テストデータは削除済み。

45. **取り込みプレビューのサムネイルを大きく表示**（項目44のスクリーンショットに対し、
    「カメラ撮影時の画像乱れや、アップロードファイルの間違いを防止するために、サムネイルを
    大きく表示してほしい」との依頼）。
    - `.monitor-import-preview-step`内のサムネイル一覧にだけ`.monitor-preview-list-large`
      という修飾クラスを追加し、`.monitor-preview-item`を140px→320px、
      `.monitor-preview-item-thumb`の高さを100px→280pxに拡大。既存登録ファイル一覧
      （`.monitor-import-existing`、参照用でクリックすると分析画面が開くだけの一覧）側は
      修飾クラスを付けていないため、従来通り小さいサイズのまま。保存前の最終確認（画像が
      ブレていないか、意図したファイルか）が必要な場面だけを狙って拡大した。
    - 動作確認：ブラウザプレビューでテスト画像をドロップし、実際に320×280pxで
      表示されることを確認済み。

46. **サムネイルのobject-fitをcoverからcontainに変更**（項目45で拡大したサムネイルが
    `object-fit:cover`のままだったため、アスペクト比が合わないと画像の端が切れて
    しまう問題があり、「画像全体が見える大きさにしてほしい」との依頼）。
    - `.monitor-preview-item-thumb img`の`object-fit`を`cover`→`contain`に変更
      （既存登録ファイル一覧・保存前プレビューの両方に影響する共通スタイル）。
      アスペクト比が枠と異なる場合は上下または左右に余白ができるが、画像全体が
      必ず見えるようになる。監視画面の端に映っている数値が見切れる事故を防ぐ狙い。
    - 動作確認：ブラウザプレビューで横長のテスト画像（四隅に色マーカーを描画）を
      ドロップし、四隅とも欠けずに表示されることを確認済み。

47. **最適運転アドバイスの機器データ部分をHTML表として表示**（ユーザーからGeminiの
    回答スクリーンショットを見せられ、「データ部分は表にすることができる？」との依頼。
    スクリーンショットには`**太字**`や`###見出し`等のMarkdown記法がそのまま文字として
    表示されてしまっている問題も見えていた）。
    - `LOAD_TABLE_OUTPUT_INSTRUCTION`という共通の指示文を新設し、
      `buildOptimalOperationPrompt`・`buildOptimalOperationMultiPrompt`の両方に追加。
      「アドバイス本文は装飾記号（**太字**・#見出し・---罫線）を使わないプレーンな文章に
      し、回答の最後の1行にだけ機器ごとの読み取り結果を
      `LOAD_TABLE: [{"name":...,"outputKW":...,"capacityKW":...,"loadPct":...}, ...]`
      というJSON配列で出力する」よう指示する（項目30・38と同じ「回答末尾に機械可読な
      1行を仕込む」パターン）。
    - `parseLoadTable(rawText)`で末尾のJSON行を抽出しつつ、本文からも念のため
      `stripMarkdownDecoration()`で`**`/`#`/`---`等の記号を除去（Geminiが指示を
      完全に守らなかった場合の保険）。
    - `LoadTableView`コンポーネントを新設。既存の`entry-list-table`スタイルを再利用し、
      機器・出力(kW)・定格(kW)・負荷率(%)の実際のHTML`<table>`として描画する。
      個別の「🤖 Geminiで分析」ボタン（画像分析+最適運転アドバイス統合、項目43）と、
      複数枚時の「🤖 最適運転アドバイス（全画面まとめて）」の両方に対応。
    - 動作確認：ブラウザプレビューで各機器の出力を明記したテスト画像をアップロードし、
      Geminiの回答から正しい数値（冷温水発生器No.1: 350/500/70.0%等）が実際の表として
      描画されること、本文に装飾記号が残っていないことを確認済み。テストデータは削除済み。

48. **データ入力フォームの数値欄にinputMode="decimal"を追加（iPadでテンキー表示）**
    （ユーザーから「データ入力時にテンキーを表示して。iPadキーボードは表示なし」との依頼）。
    - iOS/iPadOS Safariは`type="number"`だけではフルキーボード（QWERTY配列）が表示される
      ことがあり、数字だけのテンキー（デシマルパッド）が出ないことがある。`inputMode="decimal"`
      を明示することで、数字とピリオドのみのテンキーを表示させる標準的な方法に対応した。
    - データ入力画面（`DataEntryView`内、`ENTRY_FIELDS`をmapしている箇所）の数値欄
      （外気温度・外気湿度・冷水/温水負荷・受電電力量・重油使用量×4項目、計9項目）すべてに適用。
      設定画面（`SettingsModal`）側の数値欄は今回のスコープ外として変更していない
      （ユーザーの依頼が「データ入力時」に限定されていたため）。
    - 動作確認：ブラウザのDOM上で9つの数値欄すべてに`inputmode="decimal"`が正しく
      設定されていることを確認済み。実機のiPadでの表示自体はブラウザ環境の制約上
      検証できていない。

49. **「レポート作成(PDF)」機能を削除**（ユーザーから「レポート作成ボタンを削除。機能も
    削除して」との明示的な依頼）。
    - ヘッダーの「レポート作成(PDF)」ボタン、`showReportPreview` state、画面内プレビュー
      バー（`.report-preview-bar`、印刷する/プレビューを閉じるボタン）、印刷専用の
      レポートタイトル表示（`.print-only-header`）を丸ごと削除。
    - 関連CSSも全て削除：`.print-only-header`/`.print-report-title`/`.print-report-meta`/
      `.report-preview-bar`/`.report-preview-actions`/`.app-shell.report-preview`系の
      画面上プレビュー用ルール一式、および`@media print`ブロック全体（印刷時のレイアウト
      調整用CSS）。
    - `alert-banner`に付いていた`no-print`クラス（対応するCSSが消えたため無意味になった）
      も除去。`window.print()`の呼び出しもこの機能以外では使われていなかったため、
      合わせて削除された。
    - **注意**：これにより、月次/年次レポートの印刷・PDF化機能（項目14・19で実装した
      もの）はアプリから完全に無くなった。ブラウザ標準の印刷（Ctrl+P等）を使っても、
      画面表示のまま（ヘッダーやボタン類も含めて）印刷される状態に戻っている
      （印刷専用レイアウト調整は行われない）。
    - 動作確認：ブラウザプレビューでボタンが完全に無くなったこと、ダッシュボード・
      分析・データ入力の各画面が問題なく表示されることを確認済み。

50. **「分析で詳しく見る」ボタンで該当グラフまでスクロールしGemini分析を自動実行**
    （ユーザーから、ボタンを押しても分析画面に切り替わるだけでどこを見ればいいか
    分かりにくい、注意事項の表示を残したまま該当グラフとGemini分析済みの状態で
    見せてほしい、との依頼）。
    - **アラートバナーをmodeに依存させず常時表示**：これまで`showAlertBanner`の
      バナーは`mode === "dashboard"`の描画ブロック内にあり、分析画面に切り替えると
      アンマウントされて消えていた。バナーの描画をそのブロックの外（`mode`分岐の前）
      に移し、dashboard/analysis/entryどのモードでも表示され続けるようにした。
    - **アラートにtype/metricKeyを付与**：`recentAlerts`生成時、各アラートに
      `type`（`"anomaly"`か`"demand"`）を持たせ、`anomaly`の場合はさらに`metricKey`
      （どの項目で異常検知したか）も持たせるようにした。
    - **`analysisFocus` stateとAnalysisViewへのfocus prop**：「分析で詳しく見る」
      クリック時、`recentAlerts[0]`（先頭のアラート）のtype/metricKeyから
      `{type, metric, token: Date.now()}`を`analysisFocus`にセットしてmodeを
      "analysis"に切り替える。`AnalysisView`は新設した`focus`propを受け取り、
      `focus.type==="anomaly"`なら`anomalyMetric`をfocus.metricに同期した上で、
      対応するパネル（`anomalyPanelRef`=2.異常値検知、または`demandPanelRef`=
      7.デマンド分析）まで`scrollIntoView({behavior:"smooth"})`する。
      `anomalyMetric`の同期とスクロールを同じ副作用内で行う際、state更新の
      タイミング競合を避けるため、「metricが一致するまでは早期returnして
      次のレンダーで再度実行される」という2段階の設計にしている（`handledFocusTokenRef`
      で同じtokenに対する二重スクロールも防止）。
    - **AiCommentBoxに`autoRunKey`propを新設**：新しい値を受け取ったときだけ（内部の
      `lastAutoRunKeyRef`で判定）`handleClick()`を1回だけ自動実行する。対象パネルの
      `AiCommentBox`にだけ
      `autoRunKey={focus && focus.type === "anomaly" && focus.metric === anomalyMetric ? focus.token : null}`
      のように条件付きで渡し、`anomalyMetric`が実際に同期されるのを待ってから
      自動実行されるようにしている（他の10箇所の`AiCommentBox`呼び出しは
      `autoRunKey`未指定のままなので影響を受けない）。
    - 動作確認：ブラウザプレビューで「分析で詳しく見る」を押すと、バナーが残ったまま
      分析画面に切り替わり、正しい項目（受電電力量）が選択された状態で異常値検知
      パネルまで自動スクロールし、Geminiによる実際の分析コメントが自動的に
      表示されることを確認済み。

51. **分析画面でヘッダー・注意事項バナーを常時表示（スクロール追従）＋該当行のハイライト**
    （ユーザーから、分析画面をスクロールしてもヘッダーとアラートバナー（コメント）を
    残したまま、該当する項目（アラートの元になった日付の行）をバナーと同じ背景色で
    ハイライトしてほしい、との依頼）。
    - `<header className="app-header">`とアラートバナーを、新設した
      `<div className="app-sticky-top">`でまとめて包み、`position: sticky; top: 0;`
      を適用。ページ全体（`html`/`body`）がスクロールする構成のまま、ヘッダーと
      バナーだけが画面上部に張り付き続けるようにした。バナーは
      `showMonitorImport`が`true`の間（中央監視画面取り込みパネル表示中）は
      非表示のまま（元の挙動を維持）。
    - **`overflow-x: hidden`がposition:stickyを無効化する不具合**：`index.html`の
      `html, body { ... overflow-x:hidden; }`により、CSSの仕様上
      `overflow-y`が自動的に`auto`へ変換され、`body`要素が（実際にはスクロールしない
      にもかかわらず）スクロールコンテナとみなされてしまい、sticky要素が
      本来スクロールしている`html`ではなく`body`を基準に位置計算されて機能しない
      状態になっていた（`getComputedStyle`で`body`の`overflow-y`が`auto`に
      なっていることを確認して特定）。`overflow-x: hidden`を`overflow-x: clip`に
      変更することで、横スクロール防止の効果は保ったまま余計なスクロール
      コンテナ化を回避し、sticky指定が正しく機能するようにした。
    - **該当行のハイライト**：「分析で詳しく見る」クリック時に`analysisFocus`へ
      `date: first.date`（アラートの日付）も持たせるようにし、`AnalysisView`の
      異常値検知テーブル（`anomaly.list.map`）で`focus.type==="anomaly" &&
      focus.metric===anomalyMetric && focus.date===x.date`が真の行にだけ
      `row-focused`クラスを付与。CSSはアラートバナーと同じ配色
      （ライト`#fef2f2`／ダーク`#3f1d1d`）を使い、バナーとテーブル行が
      視覚的につながって見えるようにした。デマンドアラート（`type==="demand"`）は
      対応する日付単位の一覧テーブルが存在しないため、ハイライト対象は
      異常値検知テーブルの行のみ。
    - 動作確認：ブラウザプレビューでライト/ダーク両モードにて、「分析で詳しく見る」→
      該当行（例：2026-03-29）がバナーと同色でハイライトされること、ページを
      下までスクロールしてもヘッダーとバナーが画面上部に残り続けることを確認済み。

## 5. 発見・修正した重要な不具合

### 5.1 Service Workerのキャッシュ不具合（★最重要・再発しやすいので必読）
`sw.js`は当初、**全リクエストをキャッシュ優先**（`return cached || fetchPromise`、
ネットワーク応答は次回訪問時のキャッシュ更新にしか使わない）で実装していました。これだと
同一オリジンのアプリ本体ファイル（`app.compiled.js`等）は、**コードを修正して`npm run build`
しても、ブラウザには常に「1つ前のバージョン」が表示され続ける**という重大な不具合がありました
（新しいレスポンスはキャッシュに保存されるだけで、今回の表示には使われないため）。

**修正**：`sw.js`のfetchハンドラを分割し、
- 同一オリジン（アプリ本体）→ **ネットワーク優先**、失敗時のみキャッシュにフォールバック
- クロスオリジン（バージョン固定CDN）→ 従来通りキャッシュ優先（不変URLなので問題なし）

### 5.1.1 ローカル開発サーバでも似た「ビルドし直したのに反映されない」現象があった
`scripts/dev-static-server.js`（ローカルプレビュー用のNode製静的サーバ、Service Workerとは無関係）
がレスポンスに`Cache-Control`ヘッダを一切付けていなかったため、ブラウザの通常のHTTPキャッシュ
（ヒューリスティックキャッシュ）により、`npm run build`後に`navigate`で再読み込みしても
`app.compiled.js`が古いまま読み込まれ、存在しないはずの変数参照エラー（TDZ由来の
`ReferenceError: Cannot access 'x' before initialization`）が out-of-order に見えるコードから
発生しているように見える、という紛らわしい状況が発生した。Service Worker側のキャッシュを
`caches.delete()`で全消去しても直らず、新しいタブ（`tabs_create`）で開くと直った、という
挙動から「同一タブのHTTPキャッシュ」が原因と切り分けた。**対策として`dev-static-server.js`の
全レスポンスに`Cache-Control: no-store`を付与した**（`app.jsx`本体の修正ではなく、あくまで
ローカル開発サーバの設定）。今後「コードは合っているはずなのに古い動作のまま」という症状が出たら、
まず新しいタブで開き直して切り分けること。

に変更。あわせて`CACHE_NAME`を`"energy-app-cache-v1"`→`"energy-app-cache-v2"`に上げ、
`activate`イベントのクリーンアップ処理で旧キャッシュを確実に破棄するようにしました。

この不具合は、実際に「概算コスト」KPIカードが（コード上は正しく実装されているのに）
ブラウザに表示されないという形で発現し、原因特定に時間がかかりました（`bash grep`や
`fetch(...,{cache:'no-store'})` + `.includes()`によるテキスト検索が、日本語の多バイト文字や
Service Workerのインターセプトの影響で信頼できない結果を返したため——**このアプリのデバッグでは、
ファイル内容の文字列検索より、実際にブラウザでレンダリングされたDOMを
`document.querySelectorAll(...).textContent`等で直接確認する方が確実**です）。

**今後コードを修正しても画面に反映されない場合**は、まずこのService Workerキャッシュを疑い、
ブラウザの開発者ツールでService Worker登録解除＋キャッシュ全削除を試してください。

### 5.2 グラフ・予測関連
- **前期比較ON時に予測（点線）が表示されない**：`buildMainChartConfig`の予測オーバーレイ処理が
  非比較モードの分岐にしかなかったため。共通の`pushForecastDataset`関数に切り出し、比較・
  非比較どちらの分岐からも呼ぶよう修正。
- **月/年の着地予測が「実績合計より少ない」など不可能な値になる**：`computeFiscalYearForecast`の
  旧ロジックは「日数カウントで完了/未完了を判定」する簡易ヒューリスティックで、複数の月に
  未入力日があると最初の1ヶ月しか未完了として扱わず、他の未完了月をまるごと合計から
  落としてしまうバグがありました。`latestDate`が属する月のインデックス(`currentIdx`)を基準に、
  「完全に過去の月は欠落込みでそのまま合計」「現在進行中の1ヶ月だけ日単位で予測」
  「未来の月は推定」と明確に3分類するロジックに書き換えて修正。
- **年表示でも前期比較のスタイルが残る**：`buildMainChartConfig`呼び出し側で
  `compareEnabled && view !== "year"`を強制することで解消。

### 5.3 UI/レイアウト
- `.btn-ghost`（白文字）が明るい背景のモーダル内で使われて見えなくなる不具合（設定モーダルの
  キャンセルボタン、バックアップボタン、全画面モーダルの閉じるボタン等）→ 明るいモーダル内は
  すべて通常の`.btn`に統一。
- KPIグラフパネルの高さが固定されていたため、系列数増加や予測バナー表示時にグラフが
  はみ出す/切れる → 高さ固定を `.chart-canvas-wrap`（キャンバス部分のみ）に限定し、
  パネル全体は可変にした。

### 5.4 データ入力・再取込
- 「データ更新」で再取込しても画面が変わらないように見える不具合 → マージ/永続化処理自体は
  正しく動作していたが、**表示中の期間（選択中の年月/年度）が新しい最新日付へ自動的に
  切り替わらなかった**ことが原因。`handleImportData`内で、取込後に選択期間を最新日付へ
  ジャンプさせる処理を追加。
- 異常値検知のσ計算が、非数値の温度/値セルが混入した際に`NaN`を返して検知件数が黙って0に
  なる不具合 → `AnalysisView`の異常値`useMemo`と`App`の`recentAlerts`の両方に
  `!isNaN(...)`の明示チェックを追加。

## 6. 未解決の問題（対応保留・要ユーザー確認）

- **2025年度（FY2026, 2025-04〜2026-03）の冷水/温水熱負荷(GJ)が、前年度の同じ日と比べて
  おおよそ1/10のスケールになっている**一方、電力量・重油使用量は同じ期間で正常に見える、という
  データ上の疑問点があります。メーターの校正変更や元データの単位/計算式の変更などが疑われますが、
  ユーザーにはこの点を報告済みで、まだ回答をもらっていません。**憶測で補正しないこと**
  （3.2節のデータ破損対応と同じ方針）。ユーザーから説明があり次第対応してください。

## 7. 未実装（意図的に見送ったもの）

- **特定曜日への一括入力**：改善提案の一つとして挙がったが、ユーザーが「6以外を実装して」と
  明示的に除外を指示したため未実装。改めて要望があれば実装すること。

## 8. 今後の作業時の注意

- 大きな仕様変更・新機能を実装したら、このファイルにも追記してください。
- `app.jsx`は1ファイルにすべて詰まっているため、編集後はREADME.mdに記載のBabel構文チェック
  コマンドを実行してからビルドすると安全です。
- Chart.jsのグラフ関連を触る際は、`ChartCanvas`の`key`文字列に必要な依存値
  （ダークモード・比較モード・予測方式など）が全て含まれているか確認すること
  （含まれていないとグラフが古い設定のまま再マウントされない）。
